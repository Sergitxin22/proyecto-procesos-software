services:
  db:
    image: postgres
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: flexilearn
    ports:
      - "5432:5432"
    volumes:
      - postgres_flexilearn:/var/lib/postgresql
volumes:
  postgres_flexilearn:
